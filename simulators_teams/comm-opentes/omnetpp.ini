[General]
network = CoSimulationNetwork
scheduler-class = "omnetpp::cSequentialScheduler"
sim-time-limit = 1000s

# Largura de banda
**.node_0.bandwidth_bps = 50000.0

# Drop de Pacotes: 0.0 = sem perda (ideal para o benchmark com/sem controle).
# Aumente (ex: 0.15 = 15%) para estudar o impacto da perda de pacotes no controle.
**.node_0.drop_probability = 0.0

# Jitter: Injeta uma média de 50ms de atraso caótico nas filas
**.node_0.jitter_mean = 0.05